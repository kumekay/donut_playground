name: "Copilot Setup Steps"

on:
  workflow_dispatch:
  push:
    paths: [.github/workflows/copilot-setup-steps.yml]
  pull_request:
    paths: [.github/workflows/copilot-setup-steps.yml]

jobs:
  # The job name must be exactly `copilot-setup-steps` — GitHub runs it to
  # prepare the Copilot coding agent's environment before the agent starts.
  copilot-setup-steps:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # esp-donut needs python >= 3.13; ESP-IDF's install.sh builds its env
      # from whatever python3 is first on PATH, so pin it before installing.
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Cache ESP-IDF and tools
        uses: actions/cache@v4
        with:
          path: |
            ~/esp-idf
            ~/.espressif
          key: esp-idf-release-v5.5-esp32c3-py3.13-${{ runner.os }}

      - name: Install ESP-IDF (esp32c3 toolchain)
        run: |
          if [ ! -d "$HOME/esp-idf" ]; then
            git clone --depth 1 --branch release/v5.5 --recursive --shallow-submodules \
              https://github.com/espressif/esp-idf.git "$HOME/esp-idf"
          fi
          "$HOME/esp-idf/install.sh" esp32c3

      - name: Install test dependencies and the Donut client
        run: |
          source "$HOME/esp-idf/export.sh"
          pip install -r requirements-test.txt
